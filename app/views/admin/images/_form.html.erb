<% form_for @item, :url  => options, :html => { :multipart => true } do |form| %>
  <fieldset class="inputs">
    <ol>
      <%= build_form(@fields, form) %>
      <li>
        <label>
          Seccion (si es necesario)
        </label>
        <%= form.select(:section, {'Imagen para la pagina principal' => 'slide-index', 'Imagen para cupon' => 'cupon', 'Banner' => 'banner'}) %>
      </li>
    </ol>
  </fieldset>
  <fieldset class="buttons">
    <ol>
      <li><%= submit_tag button , :class => 'commit' %></li>
    </ol>
  </fieldset>
<% end %>