.banner
  - image = Image.first(:conditions => {:section => 'banner'}, :order => 'created_at DESC')
  = link_to(image_tag(image.image.url), image.url, :alt => image.description, :target => '_blank') if image